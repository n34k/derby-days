export type idP = Promise<{ id: string }>;
export type emailP = Promise<{ email: string }>;
export type priceIdP = Promise<{ priceId: string }>;
export type productIdP = Promise<{ productId: string }>;
